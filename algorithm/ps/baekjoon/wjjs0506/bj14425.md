# 제목

## 문제정보

- 사이트 : boj(백준 알고리즘 사이트)
- 문제 번호 : 14425
- 문제 분류 : 집합과 맵
- 난이도 : silver4

## 문제 풀이

### 생각한 문제 조건

1. 자료형 제한 조건

   - 1 <= N <= 10,000
   - 1 <= M <= 10,000

   > **💬 생각 노트**  
   > 해당 문제의 조건으론 이중 포문으로 해도 이상없으므로 문제의 분류인 Map을 쓰기 전에 얼마나 효율적으로 개선이 되는 지 알아보기 위해 일반 반복문을 사용해보았다.  
   > 중복이 없으므로 Map의 containsKey를 이용하여 포함된 갯수를 세었다.

### 내가 짠 코드

결과 : [메모리 : 31228KB, 시간 : 372ms]

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.HashMap;
import java.util.Map;

public class BJ14425 {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String[] input = br.readLine().split(" ");
        int N = Integer.parseInt(input[0]);
        int M = Integer.parseInt(input[1]);

        Map<String, Integer> map = new HashMap<>();
        for (int i = 0; i < N; i++) {
            map.put(br.readLine(), 0);
        }

        int count = 0;
        for (int i = 0; i < M; i++) {
            String temp = br.readLine();
            if(map.containsKey(temp)){
                count++;
            }
        }

        System.out.println(count);
    }
}
```

## 의문점 및 개선 한 코드

## 참고

### Map을 안쓰고 일반 반복문을 통한 코드

결과 : [메모리 : 31684KB, 시간 : 2816ms]

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class BJ14425 {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String[] input = br.readLine().split(" ");
        int N = Integer.parseInt(input[0]);
        int M = Integer.parseInt(input[1]);

        String[] arrN = new String[N + 1];
        for (int i = 0; i < N; i++) {
            arrN[i] = br.readLine();
        }

        int count = 0;
        for (int i = 0; i < M; i++) {
            String temp = br.readLine();
            for (int j = 0; j < N; j++) {
                if(arrN[j].equals(temp)) {
                    count++;
                    break;
                }
            }
        }

        System.out.println(count);
    }
}
```
