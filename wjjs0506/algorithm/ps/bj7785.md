# 제목

## 문제정보

- 사이트 : boj(백준 알고리즘 사이트)
- 문제 번호 : 7785
- 문제 분류 : 집합과 맵
- 난이도 :silver 5

## 문제 풀이

### 생각한 문제 조건

1. 자료형 제한 조건
   - 출입 기록 수 : 2<= n <= 10^6
   - 시간 제한 : 1초
   - 메모리 제한 : 256MB
2. 용어 정의
   - enter : 출근(회사에 있는 사람)
   - leave : 퇴근(회사에 없는 사람)
3. 문제의 다른 조건
   > **💬 생각 노트**  
   > 문제 분류가 집합과 맵이고 출입 기록 수의 최대 값이 10^6인데 시간 제한이 1초라서 Map을 통해서 구현하였다.  
   > Map 중에서는 정렬해서 출력을 해야하므로 TreeMap을 사용하여 구현하였다.
   >
   > [이유]
   >
   > 1. list는 remove() 시 전체 list 항목을 탐색해 해당되는 제일 처음 항목을 삭제하므로 갯수가 많을 수록 오래걸림
   > 2. TreeMap의 경우 Tree 형식으로 입력시에 정렬되어 입력하므로 삭제할 대상을 탐색하는데 O(logn)으로 우수한 성능을 보인다.

### 내가 짠 코드

**list로 구현한 코드**  
[결과 : 시간 초과]

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.ArrayList;
import java.util.Collections;

public class BJ7785 {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int N = Integer.parseInt(br.readLine());

        ArrayList<String> list = new ArrayList<>();
        for (int i = 0; i < N; i++) {
            String[] input = br.readLine().split(" ");
            if("enter".equals(input[1])){
                list.add(input[0]);
            } else {
                list.remove(input[0]);
            }
        }

        Collections.sort(list, Collections.reverseOrder());

        for (String s : list) {
            System.out.println(s);
        }
    }
}
```

**TreeMap으로 구현한 코드**  
[결과 : 메모리 : 57220KB, 시간 : 1076ms]

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.*;

public class BJ7785 {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int N = Integer.parseInt(br.readLine());

        Map<String, Integer> map = new TreeMap<>(Collections.reverseOrder());
        for (int i = 0; i < N; i++) {
            String[] input = br.readLine().split(" ");
            if("enter".equals(input[1])){
                map.put(input[0], 0);
            } else {
                map.remove(input[0]);
            }
        }

        for (String s : map.keySet()) {
            System.out.println(s);
        };
    }
}
```

## 의문점 및 개선 한 코드

해당 문제를 정렬 기능을 중점으로 잡아 TreeMap으로 구현하였지만,  
삽입, 삭제, 중복 제거 적 측면으로 HashSet을 사용할 수도 있다.

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int N = Integer.parseInt(br.readLine());
        Set<String> set = new HashSet<>();

        for (int i = 0; i < N; i++) {
            String[] input = br.readLine().split(" ");
            String name = input[0];
            String action = input[1];

            if ("enter".equals(action)) {
                set.add(name);
            } else {
                set.remove(name);
            }
        }

        List<String> result = new ArrayList<>(set);

        Collections.sort(result, Collections.reverseOrder());

        for (String name : result) {
             System.out.println(name);
        }
    }
}
```

## 참고

[같은 Repo 내 SET](../../../../java/wjjs0506/set.md)
